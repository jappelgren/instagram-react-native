import React from 'react';
import { ScrollView, Text } from 'react-native';

export default function Profile() {
  return (
    <ScrollView style={{ flexGrow: 12 }}>
      <Text>Hey, it's the profile view.</Text>
    </ScrollView>
  );
}
